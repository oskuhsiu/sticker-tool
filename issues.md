# sticker-tool 待解決 Issues

> 來源：high-effort 設計審查（/code-review，2026-06-09）。1–3 已修正進計畫；4–6 另行討論中；以下為其餘待處理項。

## ✅ 已修正（記錄用）
- **規格錯誤**：動態貼圖 300KB→**1MB**、張數→**僅 8/16/24**、新增「一邊須≥270」、循環**須 1–4 非無限**。已查證官方並改進計畫。
- **動態包結構**：main.png 須 APNG、tab 須靜態、靜態與動態不可混包。
- **RGB/RGBA 矛盾**：靜態改為透明 RGBA，避免 validate 誤判自家產物。

## 🔬 討論/實測中

### #4　跨呼叫角色一致性（已實測 ✅）
**問題**：不同次的 codex 呼叫（separate invocation），能守得住同一個角色外觀嗎？這是 count>單張組圖容量時（16/24 張需多張大圖或多次呼叫）的關鍵風險。
**實測**：第二次獨立呼叫（spike/run2，同 ref.jpg 產 6 個同角色不同表情），與 run1 並排（spike/consistency_compare.png）。
**結論（修正，使用者判讀）**：**跨呼叫「身份沒守住」——是「不同人穿同一套衣服」。**
- ✅ 守住的只有：戲服（綠葉領）＋畫風＋大致色調。
- ❌ 沒守住：**臉（人）**——鼻子/眼型/臉型每張都變，並非同一人。額頭徽記也漂（金星→花）。
- 對照：run1 的 12 格（**同一次呼叫**）臉是一致的 → **同次呼叫內身份守得住，跨呼叫崩。**

**硬約束（架構級）**：
- **一個角色貼圖包必須在「同一次 codex 呼叫」內產完**；跨呼叫拼包 = 一包裡好幾個人，不可接受。
- 多次呼叫把第一張當 `-i` 參考餵後續，**仍只傳戲服+畫風、傳不住臉**，故非可靠解。

**決策（使用者定案：直接定策略，不再測）**：
1. **AI 角色包一律「單次 codex 呼叫、單張組圖」產完所有貼圖**——這是保證同一人的唯一可靠手段。
2. **張數實務上限**（基準：spike 4×3=12 格品質佳）：
   - **8 張**：完全支援（品質佳）。
   - **16 張**：支援，但**警告單格解析度下降**（4×4，超出已驗證範圍）。
   - **24 張**：**預設擋下並警告**。選項：(a) `--force` 接受降質強制單次、(b) 改 8/16 張、(c) 改逐張高解析模式（臉會漂，僅適合非角色/物件貼圖）。
3. **多次呼叫/多張大圖僅用於非角色貼圖**（物件、文字、風景等不需身份一致者）。
4. prompt 仍鎖死特徵（如「金色五角星」）+ 提供重生迴圈，提升單次品質。
**待納入計畫**：grid.ts 的張數→單張大圖配置 + 上限檢查與警告；codexGen 標記「角色包=單次」。

### #5　codex 非決定性的處置
**問題（原審查 issue）**：把「組圖→切格→去綠幕→佈局」委派給 LLM agent，每次呼叫可能改檔名/座標/格數或失敗；無重試/逾時/冪等/續跑策略，會間歇炸裂且難重現除錯。
**回覆/建議**：核心原則是**別讓 codex 掌管「確定性」步驟**，職責切開：
- **codex 只負責「畫」**（唯一不可取代的創意）：產一張組圖、用指定綠幕色、存到**指定固定檔名/路徑**、用**指定 rows×cols**。
- **工具（TS，確定性）負責其餘**：等分切格（位置已知，網格由我們規定）、去綠幕、fit、驗證、打包。

5 道防線把不確定性關進籠子：
1. **輸出契約固定**——指定確切檔名，事後檢查存在；缺檔/多檔就重試。
2. **`--output-schema`**（codex exec 支援）讓 codex 回傳結構化 JSON（sheetPath/cols/rows/bgColor），工具不必用猜的。
3. **產出驗證**——尺寸/格數/透明度先驗過再往下；不符就重試 N 次（精煉 prompt）。
4. **冪等 --out**——在 temp 目錄做、成功才 atomic move；`--resume` 跳過已 OK 的大圖。
5. **一致性**——後續呼叫一律把「第一張組圖」當 `-i` 參考餵進去（即 #4 在測的）。

→ 效果：codex 的隨機性只剩「像素長相」，結構與規格留在 TS 確定性流程。**待納入計畫。**

### #6　工具鏈可攜性 / Python vs JS on mobile
**問題**：本來要用 JS，但你看到現成 Python 工具就直接用了；想知道在 mobile 上用 Python 跟 JS 的難度差多少？
**回覆**：
- **先澄清**：Python 只出現在 spike（拋棄式實驗，因 Pillow/ffmpeg/pngquant 機器現成、驗證最快）。**產品工具是 Node/TS，Python 不會進產品依賴。**
- **難度對比（mobile / React Native）**：

  | | JS/TS | Python |
  |---|---|---|
  | 在 RN 跑 | 原生支援，直接跑 | 要內嵌 CPython（Chaquopy/BeeWare）、app 肥 +10~30MB、build 複雜 |
  | 影像庫 | native binding 仍要換（sharp 不能直接上 RN），但有 skia/expo 替代 | Pillow 是 C 擴充，每架構交叉編譯，極痛 |
  | 難度 | ~2/10 | ~8/10 |

- **真正的啟示**：不論 JS 或 Python，**重型影像工具（sharp、pngquant、apngasm、@imgly、Pillow）沒有一個能乾淨搬上 mobile**——只有純 TS 的**邏輯核心**（規格/驗證/命名/grid/prompt）能共用。選 JS 不是為了搬影像處理，而是為了搬**邏輯**並貼近 RN。
- **連動 I-6 選型矛盾**：mobile 要可攜就該用純 JS 的 upng-js；apngasm/pngquant 這類原生 binary 注定只能留在 CLI。

## 🟡 待處理

### I-7　中文字型渲染脆弱
sharp+SVG 靠 fontconfig 解析 'Noto Sans TC'，缺字型會**靜默 fallback 成豆腐、不報錯**。
- 後果：他人機器/CI 上中文字全毀，到肉眼檢查才發現。
- 解法方向：**內嵌字型檔**（指定 .otf 路徑、SVG 內嵌或 path 化），不依賴系統字族名；找不到字型須**報錯**而非靜默。

### I-8　@imgly 去背的離線性與可攜性
@imgly/background-removal-node 首跑需從 CDN 下載 onnx 模型（非真離線）；部分 runtime 有 segfault 回報；無法直接跑在 RN。
- 後果：「本地免費去背」首次需網路；mobile 去背須換完全不同實作，兩平台輸出可能不一致。
- 解法方向：模型檔隨工具打包或首次快取後鎖版；mobile 去背列為平台專屬重寫項；評估改用 codex 去綠幕為主、@imgly 僅 local 來源備援。

### I-9　靜態規格只「驗」不「修」
單張 >1MB / 長寬非偶數 / 10px 邊距，計畫只驗證無補救。
- 後果：高細節去背圖壓出 1.2MB → validate 報錯卻無自動降階，使用者卡死。
- 解法方向：靜態也給輕量 auto-fit（pngquant 降階至 ≤1MB）；偶數修正方向固定（統一 −1 或 +1 並重算置中）；明定 10px 邊距為強制 padding。

### I-10　兩個未定義行為
1. 去背 `auto`「偵測殘留背景」**無判據**（邊緣不透明像素比例？四角取樣閾值？）——無判據無法實作。
2. CLI 參數 vs 設定檔**優先序未定**（build --count 8 vs config count:24 誰贏）；缺漏欄位預設來源；列舉值非法字串行為。
- 解法方向：定義殘留判據（如四角 + 邊緣取樣 alpha>閾值比例）；明定 CLI 覆寫 config、config 覆寫 spec 預設；非法值報錯。

## 💡 精簡建議（過度設計）

### S-1　detect 裁切策略應砍、cropGrid/removeBackground 與 codex 重複
spike 證明 codex 能自切等分格+去綠幕。自有 detect（物件偵測切割）複雜度高且用不到；cropGrid 三策略+@imgly 大量重做 codex 已完成的事。
- 建議：AI 路徑信任 codex 輸出、工具只驗證；detect 砍除；自有 cropGrid 僅保留給「使用者自備非 codex sheet」的 local 分支（equal 即可）。

### S-2　里程碑排序倒置
使用者主意圖是 AI 生圖（唯一已驗證能跑），卻把本機靜態排 M1。
- 建議：M1 = 共用後段（spec/validate/naming/fitCanvas/buildMainTab/buildZip）+ 直接串已驗證的 codexGen，先打通 AI 端到端；local 去背降為後續分支。

### S-3　core/ 為未做的 mobile 預先抽象
連 CLI 都還沒寫，mobile 是 someday/maybe；硬性平台分層是臆測性抽象。
- 建議：保留自然的 spec/types/validate 純函式分檔（零成本好品味），拿掉「為 mobile 而抽象」的硬約束與 prompt 層特殊地位；mobile 真做時依實況重構。
